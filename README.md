# flowGate-openCyto

This repository is being used to test out modifications/improvements to the existing flowGate/openCyto implementations. My main interest at the time of the initial testing

1) Writing out an exisiting GatingSet to an openCyto template (which would permit reuse of the flowGate Shiny to draw initial, then use the template for read in for equivalents). 

2) Parse out exactly how to adjust individual existing gates for individuals where it is off. I suspect it is within the gs_pop_get_gate and recompute() elements, but never tinkered far enough to determine this. 

Best of luck!